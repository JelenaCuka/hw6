import React from 'react';
import './SimpleWrapper.scss';


const SimpleWrapper = (props) => {
    return (
        <>
        {props.children}
        </>
    );
}

export default SimpleWrapper;
//I'll see DELETE OR NOT